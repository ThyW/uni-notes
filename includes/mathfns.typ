#let vecNotation(term) = {
  [$accent(term, arrow)$]
}
#let above(term) = {
  $accent(term, macron)$
}

#let scalarSucin(term) = {
  [$angle.l term angle.r$]
}

#let vecSize(term) = {
  [$bar.v.double term bar.v.double$]
}
